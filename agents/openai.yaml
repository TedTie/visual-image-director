interface:
  display_name: "Visual Image Director"
  short_description: "Plan, preview, generate, and code-render PNG images."
  default_prompt: "Use visual-image-director to turn my poster or image request into design.md and prompt.md, confirm them, create a thumbnail preview, then generate, code-render, export, or locally revise the final high-resolution PNG."
